small:
  size: 30px
  hold_action:
    action: more-info
  styles:
    card:
      - height: 100px
      - font-size: 14px
    name:
      - height: 30px
  state:
    - value: 'on'
      styles:
        card:
          - background-color: var(--primary-color)
          - color: black
        icon:
          - color: black
    - value: 'home'
      styles:
        card:
          - background-color: var(--primary-color)
          - color: black
        icon:
          - color: black
